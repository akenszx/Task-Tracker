# Server
PORT=4000

# MySQL connection
DB_HOST=localhost
DB_PORT=3306
DB_NAME=task_tracker
DB_USER=root
DB_PASSWORD=your_password_here

# JWT
JWT_SECRET=replace_this_with_a_long_random_string
JWT_EXPIRES_IN=7d

# CORS - the deployed frontend origin (Vercel URL). Comma-separate multiple origins.
CORS_ORIGIN=http://localhost:5173
